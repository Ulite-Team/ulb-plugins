[package]
name = "android-plugin"
description = "The ulite/android plugin: compiles a Java module against the Android SDK platform jar and discovers the SDK toolchain"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
ulb-plugin-sdk = { path = "../../Uliab/crates/ulb-plugin-sdk" }
wit-bindgen = "0.51"

[lints]
workspace = true
