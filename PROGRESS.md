# Progress

## Done
- [x] Repo scaffold: workspace root, AGENTS.md, PROGRESS.md (2026-08-13)
- [x] hello-plugin: exports the ulb-plugin world, builds to a runnable component, verified end-to-end in CI (2026-08-13)
- [x] Plugin registry: `registry/index.json` committed per the format now specified in Uliab ARCHITECTURE.md §3.6 (`schema_version` / `plugins[name].versions[v].abi{min,max}` / `artifact_url`); hello-plugin's manifest name aligned to its registry coordinate (`ulite/hello`); CI `registry-resolve` job exercises the full `uliab plugins resolve` path offline (libs.ulb → index → download → real manifest verification → cache write → cache hit → `run` the cached component) plus a well-formedness check on the committed index (2026-08-14)
- [x] ABI 0.2 re-sync: hello-plugin gains the `configure` export (full-world instantiation requires it), its manifest now reports `tools` and the SDK's `ABI_VERSION`, and `export!` is wasm-target-guarded so the host build passes; version bumped to `0.2.0` in Cargo.toml, `registry/index.json` (version row, ABI range, release-asset tag), and the CI resolve job's seeded index. Verified locally end-to-end: `uliab plugins resolve` → `(registry)` → `(cache)` → `uliab run` (2026-08-15)
- [x] `ulite/jvm` plugin — plain-Java slice (ARCHITECTURE.md §5.1): compiles the module's `jvm { sources / classesDir / jarFile }` block with a `compile` javac task and packages the jar with an `assemble` jar task (depends on `compile`), consuming the host-injected `classpath.compile` jars and resolving block paths against the injected `projectDir`. Ships `jvm-plugin/REFERENCE.md` (plugin-owned keys, §5.1) and host-side unit tests for the task-argument builders. Core support landed in Uliab in the same milestone: `projectDir` added to the configure JSON (with a fixture `mkdir` probe test) and `javac`/`jar` granted on the driver's executor allowlist. Verified locally end to end from a scratch project: source → `build/app.jar` (2 ran, 0 up-to-date), unchanged rebuild (0 ran, 2 up-to-date), source-touch rerun (2 ran, 0 up-to-date), and a build invoked from a different working directory; configure rejects an empty `sources` list with a clear error. CI gains a `jvm-build` job mirroring the same flow (2026-08-15)
- [x] `ulite/jvm` — Kotlin and tests (remaining §5.1 surface minus per-scope scopes): `sources` now accepts `.java` and `.kt`, partitioned into a `compile` javac task and a `compile-kotlin` kotlinc task (kotlinc sees the compiled java classes via the classes dir on its classpath and waits for `compile` when both coexist); an optional `testSources`/`testClassesDir`/`testClass` trio (all-or-none) adds `compile-tests` (javac, depends on the main compile tasks) and `test` (`java -cp <testRuntime:testClassesDir:classesDir>`, depends on `compile-tests`); the manifest declares `kotlinc` and `java` and the plugin version bumps to `0.2.0` (wasm + Cargo + registry-facing documents). Host-injected `classpath.testCompile`/`classpath.testRuntime` are read when present and default to empty. Verified locally: mixed java+kotlin+test module (5 ran: compile, compile-kotlin, compile-tests, test, assemble; jar holds both languages' classes; the test run writes a marker file), unchanged rebuild (0 ran, 5 up-to-date), a real source change reruns all 5, kotlin-only module (2 ran), kotlin test source rejected with a clear configure error. CI `jvm-build` now installs kotlinc and runs the Java-with-tests scenario (4 ran / 0-4 up-to-date / source change reruns 4 with the marker file updated) plus a kotlin-only module scenario (2 ran). Note: deleting a task's output dir out from under a fingerprint does not invalidate it (host hashes inputs, not output existence) — observed, not fixed (2026-08-15)
- [x] `ulite/jvm` — scoped classpaths verified end to end + framework-runner test invocation: added an optional `testArgs` list appended after `testClass` on the `test` task's `java` command, so the class can be a framework runner (`org.junit.runner.JUnitCore`) that receives the test classes it should execute; plugin bumps to `0.3.0`. No host change was needed — the core resolver already implements the scope→bucket semantics (`implementation`/`runtimeOnly`/`compileOnly`/`testImplementation`/`ksp`), and the plugin already consumed `classpath.compile`/`testCompile`/`testRuntime`; this milestone proves the wiring. Verified locally and in the exact CI script body: `deps { implementation slf4j; testImplementation junit+hamcrest }` resolves to slf4j on compile/testCompile/testRuntime with junit absent from the main compile bucket (asserted from `uliab deps resolve` output), a JUnitCore suite runs green (4 ran: compile, compile-tests, test, assemble), a broken assertion fails the build with a JUnit failure, and removing the `implementation` dep breaks main compilation (testImplementation does not leak). CI gains a `jvm-scoped-classpath` job running the same scenario (2026-08-15)- [x] 
 `ulite/jvm` — generated test-runner discovery: `testSources`/`testClassesDir` now pair with exactly one run target — `testClass` (+ optional `testArgs`) or `testRunner = "junit-platform"` (mutually exclusive, both absent rejected, unknown runner rejected). The junit-platform mode registers a `generate-test-runner` task that writes a JUnit Platform Launcher-API runner (`ulite.TestRunner`) to `build/generated-test-src/ulite/TestRunner.java`, feeds it to `compile-tests` ahead of the test sources, and runs it over the test classes directory, so no console-standalone jar or explicit class list is needed; it exits non-zero when any test failed or errored, and it is agnostic to which engine/version the test classpath resolves. Plugin bumps to `0.4.0` (wasm + Cargo + registry-index entry + workflow pins), and `hello-plugin` joins it at `0.4.0`/ABI `0.4` so all three jobs resolve under the current host ABI. Verified locally end to end: `deps { implementation slf4j; testImplementation launcher + jupiter }` resolves with junit on testCompile only (asserted bucket by bucket from `deps resolve` output), first build 5 ran with the generated runner file and class present, unchanged rebuild 0 ran, a broken assertion fails the build with `AssertionFailedError`, restoring it reruns only the test chain (2 ran, 3 up-to-date), convergence back to 0 ran, and the jar holds `App.class` but no test classes. CI gains a `jvm-runner-discovery` job running the same scenario with a single cache dir (2026-08-15)- [x] 
 `ulite/jvm` — KSP invocation (generate → compile → package ordering): a module whose `deps {}` block declares `ksp` deps gets a `ksp` task that runs the real KSP2 command-line tool (`com.google.devtools.ksp.cmdline.KSPJvmMain`) over the module's Kotlin sources, then feeds the generated Kotlin directory into `compile-kotlin` so the ordering is ksp → compile → package. The `ksp` declarations resolve into the host-injected `classpath.processor` bucket, which doubles as the tool's `-cp` and its trailing processor classpath (how KSP discovers `ProcessorProvider` services); the compile classpath goes to `-libraries`. A module with ksp deps but no `.kt` sources is rejected at configure time. Plugin bumps to `0.5.0`. Host side (landed in Uliab, same milestone): a repeatable `--repo` flag on `build` and `deps resolve` that prepends custom maven repos ahead of Google/Maven Central, and a larger response-body limit so the tens-of-megabytes KSP2 toolchain jar (`symbol-processing-aa`) can download. Delivered with an in-repo fixture processor (`fixtures/ksp-hello/`, real symbol-processing-api, ServiceLoader-registered) that generates a Kotlin file referencing every `@ulite.hello.Hello` class, built by `build-fixture.sh` into a file:// maven layout for local/CI E2E. Verified locally end to end: `deps { implementation stdlib + fixture; ksp fixture; ksp symbol-processing-aa }` resolves the full toolchain into the processor bucket, first build 3 ran (ksp, compile-kotlin, assemble) with the generated source + class present, the app runs printing `[hello from ksp for Main]`, unchanged rebuild 0 ran, source change reruns all 3. CI gains a `jvm-ksp` job mirroring the same scenario (2026-08-15)

- [x] Public hosting + release-asset publishing (2026-08-16): repo made public, `registry/index.json` serves `ulite/hello` 0.4.0 + `ulite/jvm` 0.5.0 rows whose `artifact_url` fields point at GitHub release assets (`hello-plugin-v0.4.0` → `hello_plugin.wasm`, `jvm-plugin-v0.5.0` → `jvm_plugin.wasm`), and the core tool's default registry URL resolves the index with no `--registry` override. Verified end-to-end from a scratch project: `uliab plugins resolve` prints `(registry) → (cache)` and `uliab run` prints the hello banner.

- [x] `ulite/android` — compile slice (roadmap 8b, first cut): the `android-plugin` crate (0.1.0, ABI 0.4) validates an `android { compileSdk / sources / classesDir / sdkDir? }` block, resolves the SDK root (block `sdkDir` → host-injected `androidSdkDir` → configure error), discovers `<sdk>/platforms/android-<N>/android.jar` and the highest `build-tools` release carrying both `aapt2` and `d8` (both fail at configure time), and registers a single `compile` task that runs javac against the platform jar (host `classpath.compile` jars following it). Core support landed in Uliab in the same milestone: `--android-sdk` CLI flag, `BuildOptions.android_sdk`, `androidSdkDir` injection from `--android-sdk` → `ANDROID_HOME` → `ANDROID_SDK_ROOT` → `~/Android/Sdk`, and a read-only preopen of the resolved SDK root into each plugin's WASI filesystem at its real path (the guest has no other filesystem, so discovery had to become a host capability rather than plugin-side probing of nothing). Verified locally end to end: a module importing `android.app.Activity` compiles against the real android-36/android.jar (1 ran, 0 up-to-date → 0 ran, 1 up-to-date), a `compileSdk` the SDK lacks fails configure with a clear error, and a hermetic fake SDK (empty zip as android.jar, aapt2/d8 markers in 35.0.0 and 36.0.0) proves the same wiring without a real SDK. CI gains an `android-build` job mirroring the fake-SDK scenario; registry row + release.yml line added (2026-08-16)
- [x] Review fixes (2026-08-16): a module-declared `sdkDir` now works for real — the host preopens that path too (resolved against `projectDir` when relative), so a module targeting a different SDK than the host's root is discoverable from the guest instead of failing NOTCAPABLE. The plugin now resolves a relative block `sdkDir` against `projectDir` like every other block path (it previously used the raw string, which could not have matched the host's preopen of the resolved path); `resolve_sdk_root` takes the project dir and a unit test pins the relative-resolution rule. The `plugin-build` workflow gains a `pull_request` trigger so PRs get CI signal; the `android-build` job still checks out Uliab `main` and only goes green once the Uliab SDK PR lands (documented in ci.md). E2E-verified: module `sdkDir` = real SDK while `--android-sdk` points at a different root compiles against the module SDK; a relative `sdkDir` inside the project discovers and compiles against a copied real android.jar. 15 plugin unit tests, clippy/fmt clean.

- [x] `ulite/android` — packaging slice (roadmap 8b, second cut, 2026-08-16, `f668e2c`): the android-plugin crate (0.2.0, ABI 0.5) grows the compile slice into the full packaging chain. `configure` now requires `minSdk`, `namespace`, `manifest`, `resDir`, and `apk` (targetSdk defaults to compileSdk) and registers nine tasks on top of `compile`: `prepareBuildDir` mkdirs the derived build dirs (aapt2 and d8 refuse to create their own parents), `mergeResources` runs `aapt2 compile --dir res` + `aapt2 link` to merge the res tree with the manifest into a resources-only apk (generating `R.java` under the module namespace via `--custom-package`), `prepareApk` seeds the module's apk from that resources apk, `jarClasses` jars the compiled classes (d8 accepts archives, not directories), `prepareDex` pre-creates d8's output dir, `compileDex` runs d8 with the platform jar as `--lib`, and `packageApk` grafts `classes.dex` onto the apk via `jar uf`. The javac task pins `--release 17` (d8 rejects newer class files) and puts the generated R directory on `-sourcepath` so `R.java` compiles regardless of host JDK. Build-tools discovery now also validates `lib/d8.jar`. `registry/index.json` gains the 0.2.0 row (ABI `0.5–0.5`, release asset URL), `release.yml` publishes `android-plugin-v0.2.0` → `android_plugin.wasm`, and the CI `android-build` job swaps its fake SDK for a real one (sdkmanager-installed `platforms;android-36` + `build-tools;36.0.0`) and asserts apk contents (`AndroidManifest.xml`, merged `res/...`, `resources.arsc`, `classes.dex`), R.java presence, the incremental cascade counts (9/0/7/4), and that `compileSdk 99` fails configure. Verified locally end-to-end against the real SDK: 9 ran → 0 ran → 7 ran (res edit) → 4 ran (source edit). 18 plugin unit tests, clippy/fmt clean.
- [x] `ulite/android` — packaging slice review fixes (2026-08-16, committed `239e826` on the same PR): of CodeRabbit's five findings, four were valid and fixed — a supplied non-integer `targetSdk` is now a configure error (`optional_int` → `Result<Option<i64>, String>`; previously it silently defaulted to compileSdk), `packageApk` depends directly on `prepareApk` (the seeded apk is its input; previously both tasks claimed the same output with only a wave-depth coincidence ordering them) and packages every `classes*.dex` d8 emits (`jar uf <apk> -C <build>/dex .`, replacing the `classes.dex`-only vector), and a new `prepareApkDir` mkdir task creates the configured apk's parent before the copy (the apk may live outside `build/android`, which aapt2 does not create). The fifth finding (aapt2 allowlist absent) was a false positive against Uliab `main`, which lacked the not-yet-merged packaging PR; the ABI 0.5 host has `AllowlistedTool::Aapt2` with the build-tools-dir convention. Task count is now 10; local E2E re-confirmed clean 10 ran / 0 up-to-date, unchanged 0/10, res edit 7/3, Main.java edit 4/6 with convergence back to 0/10, and the apk living in `dist/` outside `build/android`. 19 unit tests, clippy/fmt clean.
- [x] `ulite/android` — packaging slice merged + published under ABI 0.5 (2026-08-16): PR #2 landed on main, then `e5562ea` widened `ulite/hello` 0.4.0 and `ulite/jvm` 0.5.0 index rows' ABI max to 0.5 (they were capped at 0.4, so the 0.5 host refused them) and dropped the `ulite/android` 0.1.0 row plus its release.yml line (no 0.1.0 tag was ever published, and the workflow was uploading the current 0.2.0-built wasm into that tag, mislabeling it). The release workflow then rebuilt all three plugins against the 0.5 SDK and published `hello-plugin-v0.4.0`, `jvm-plugin-v0.5.0` (clobbered), and the new `android-plugin-v0.2.0` release. Verified end-to-end from the real registry (raw.githubusercontent index, fresh cache, local ABI 0.5 host): hello `plugins resolve` → `(registry)` + `run` prints the banner; a jvm module builds `build/app.jar` (2 ran); an android module builds through the full 10-task packaging chain against the real SDK (apk holds manifest, resources.arsc, classes.dex). ABI 0.5 is now the coherent published state.

- [x] `ulite/kmp` — jvm-target slice (roadmap 8c, 2026-08-16): the `kmp-plugin` crate (0.1.0, ABI 0.5) compiles a multiplatform module's `commonMain` and `jvmMain` source sets into a jar. A `kmp {}` block holds source sets (blocks with `sources`/`deps`) and target configs (`jvm { classesDir / jarFile }`); unknown source-set names, non-jvm targets, entries that are neither, and non-`.java`/`.kt` source files all fail configure with named errors. `compile` runs javac over the `.java` files, `compile-kotlin` runs kotlinc over the `.kt` files (waiting for `compile` when both coexist, with the classes dir on its classpath so Kotlin resolves the module's own Java classes), and `assemble` packs the classes into the jar. Classpaths come from the host's `classpathSourceSets` (`kmp.commonMain` + `kmp.jvmMain`, merged in hierarchy order, deduped); the module's own top-level `deps {}` block is deliberately not consumed, and a CI assertion proves a module-level dep cannot satisfy a source-set import. Verified locally and in the exact CI script body against a real kotlinc (2.2.0): a module whose commonMain Kotlin imports a source-set dep and whose jvmMain mixes Java+Kotlin builds 3 ran (compile, compile-kotlin, assemble), the jar holds both source sets' classes plus the Java class, and the packaged main prints the shared dep's value at runtime; unchanged rebuild 0 ran; `deps resolve` prints `kmp.commonMain:`/`kmp.jvmMain:` sections. CI gains a `kmp-build` job mirroring the scenario; registry row (`kmp-plugin-v0.1.0` → `kmp_plugin.wasm`) + release.yml line added. 9 plugin unit tests, clippy/fmt clean. Interlock: the job checks out Uliab `main`, which carries per-source-set `deps {}` resolution once the Uliab PR for it lands.

- [x] `ulite/android` — build variants (roadmap 8d): the android-plugin crate computes the variant matrix from `buildTypes {}` and `productFlavors {}` (cartesian product; defaults to `[debug, release]` when absent). Per-variant tasks are suffixed with the PascalCase variant name (`compileDebug`, `linkResourcesReleaseFree`, etc.) and produce APKs at `<project>/build/<variant>/app-<variant>.apk`. `linkResources` is per-variant: when a flavor carries `applicationIdSuffix`, the task passes `--rename-manifest-package <namespace><suffix>` to `aapt2 link`. Flavor `minSdk` overrides the base `android.minSdk` for `d8 --min-api` and `aapt2 --min-sdk-version`; `targetSdk` flows from the base block. `compute_variants` parses `buildTypes {}` (or defaults), `productFlavors {}` (with `dimension` at the block level and per-flavor overrides), and returns a `Variant` struct per cell. Signing remains shared across all variants. 25 unit tests, clippy/fmt clean.
- [x] `ulite/android` — Kotlin support (2026-08-22): the android-plugin crate (0.3.0, ABI 0.7) now compiles mixed Java+Kotlin and Kotlin-only modules. `sources` accepts both `.java` and `.kt` files. `partition_sources` splits them; `compileJava` (javac, always registered) compiles user `.java` plus the aapt2-generated `R.java`; `compileKotlin` (kotlinc, registered when `.kt` sources exist) depends on `compileJava` and sees the classes dir on its classpath so it resolves the module's own Java classes and R references. The manifest declares `kotlinc`. `reject_unknown_extensions` accepts `.java` and `.kt`. 30 unit tests (added `partition_sources` edge cases, `kotlinc_android_args` invocation tests), clippy/fmt clean. CI `android-build` gains a Kotlin-only end-to-end scenario: `.kt` source referencing `R.layout`, full packaging chain, APK contains `classes.dex`.
- [x] `ulite/android` Kotlin review fixes (2026-08-22): (1) CI workflow uses `$GITHUB_WORKSPACE` absolute path for plugin wasm copy (was relative `../ulb-plugins/` which failed after `cd /tmp/android`), (2) CI adds DEX-level assertion verifying `Lcom/example/ulite/Main;` class is present in the packaged dex (not just that `classes.dex` exists), (3) `d8_args` accepts `extra_jars` parameter; when Kotlin sources exist, `kotlin-stdlib.jar` is resolved from the compile classpath and included in D8 program inputs so the APK contains Kotlin runtime classes, (4) compile order documented — Java compiles before Kotlin because R.java must be compiled first for kotlinc to resolve R.* references; mutual Java↔Kotlin references beyond R.java need multi-pass compilation (future work). 31 android plugin unit tests, clippy/fmt clean.
- [x] `ulite/android` — Compose compiler plugin support (2026-08-22): the android-plugin crate reads a `compose = true` key inside the `android {}` block. When enabled, the compose compiler plugin JAR is located on the compile classpath via `find_compose_compiler_jar` and loaded via `-Xplugin=<path>` — the standard Kotlin CLI contract for compiler plugins. The kmp plugin parses `android.compose` from its config and applies the same mechanism. The `d8_merge_args` function gains an `extra_jars` parameter for kotlin-stdlib inclusion in the kmp mergeDex task, and the extra JARs are declared as task inputs for proper fingerprinting. 7 unit tests added for compose flag parsing, JAR discovery, and kotlinc arg generation (`bool_value` helper for optional boolean keys). 39 android + 14 jvm + 24 kmp = 77 tests, clippy/fmt clean.
- [x] `ulite/android` — BuildConfig generation (2026-08-25): the android-plugin crate (ABI 0.7) generates a `BuildConfig.java` source file per variant during configure. Nine default fields (APPLICATION_ID, BUILD_TYPE, DEBUG, FLAVOR, VERSION_CODE, VERSION_NAME, MIN_SDK_VERSION, TARGET_SDK_VERSION, COMPILE_SDK_VERSION) are populated from the `android {}` block and variant's effective values. User-defined fields are declared via `buildConfigField ["type", "name", "initializer"]` list triples inside the `android {}` block. The generated file lives at `<build>/<variant>/generated/buildconfig/<namespace>/BuildConfig.java` and is added to `javac`'s `-sourcepath` so it resolves at compile time. Task `generateBuildConfig<V>` depends on `prepareBuildDir` and is a dependency of `compileJava<V>`. Also fixed `merge_variant_sources` to resolve base paths before deduplication. 50 android plugin tests, clippy/fmt clean.
- [x] Phase 16B — migrate all four plugins to `#[derive(UlbConfig)]` (2026-08-26): each plugin now embeds its typed config schema in the wasm custom section via `embed_schema!`. hello-plugin: `PluginConfig` unit struct (schema structurally correct, no fields). jvm-plugin: `PluginConfig`/`Classpath`/`JvmConfig` structs with full typed deserialization; removed `classpath_bucket`/`optional_string_list` helpers. android-plugin: `AndroidPluginConfig`/`AndroidBlock`/`SigningBlock`/`ClasspathBlock` structs for schema generation; `configure()` keeps `serde_json::Value` reads because `compute_variants` deeply integrates with the raw value for dynamic `buildTypes`/`productFlavors` maps. kmp-plugin: `KmpPluginConfig` struct with `serde_json::Value` for dynamic `kmp`/`classpathSourceSets`/`android`/`buildTypes`/`productFlavors`/`signing` fields. Requires Uliab >= 0.8 (PR #33). 90+ tests pass, clippy/fmt clean. PR #12.
- [x] `ulite/jvm` — desktop Compose path + compose error rewording (2026-08-29): `JvmConfig` gains `compose` (optional bool). When `jvm.compose = true`, the compile-kotlin task locates `kotlin-compose-compiler-plugin` on the compile classpath (host now injects it alongside the BOM-managed `runtime`/`ui`/`material3` deps) and passes it via `-Xplugin=<jar>`. The compose logic lives inside the Kotlin-compile branch, so a module with `jvm.compose` but no `.kt` sources simply gets no `-Xplugin` (nothing to annotate) rather than a configure error — consistent with docs/jvm-plugin.md. New helpers `kotlinc_args`/`find_compose_compiler_jar`/`map_compose_error`; 6 new jvm-plugin tests. The android/kmp "compose = true but no compiler jar on the classpath" configure errors no longer tell the author to hand-declare the artifact — the host injects it, so the message now describes host-injection failure. docs/jvm-plugin.md documents the `compose` key and the `-Xplugin` invocation. Requires Uliab compose-compiler injection (see Uliab PROGRESS "Turnkey Compose"). 18 jvm / 53 android / 24 kmp tests, clippy/fmt clean.
- [x] ABI 0.8 re-sync (2026-08-29): Uliab `main` moved the SDK to ABI 0.8 (PR #40), but every CI E2E job still seeded a local registry index whose plugin rows declared `{min: 0.7, max: 0.7}`, so Uliab's ABI-0.8 host refused them (`no published build of 'ulite/*' supports plugin-ABI '0.8'`). Widened the eight E2E-seeded index rows in `.github/workflows/plugin-build.yml` (hello/jvm/kmp/android, `max: 0.7` → `max: 0.8`) and the four newest rows in `registry/index.json` (`hello@0.5.0`, `jvm@0.6.0`, `android@0.3.0`, `kmp@0.3.0`) so each covers host ABI 0.8 and the manifest-reported `ABI_VERSION` 0.8. Kept `min: 0.7` (plugins still require the 0.7+ host that carried the `UlbConfig` migration) and left older rows at their original caps. docs/registry.md and docs/architecture.md (kmp crate version 0.2.0→0.3.0) synced to match.
- [x] `ulite/jvm` — typed config serde-key fix (2026-08-29): the jvm-plugin type-deserializes `PluginConfig`, but `#[ulb(rename = "...")]` only drives the emitted schema, not serde's field lookup, so fields whose DSL key is camelCase and Rust name is snake_case (`projectDir`/`project_dir`, `classesDir`/`classes_dir`, `jarFile`/`jar_file`) failed to deserialize once the host re-tested it at ABI 0.8 (`missing field 'classes_dir'`). Added the matching `#[serde(rename = ...)]` to those three fields. Unmasking: this predates the compose work — introduced by the #12 UlbConfig migration, but CI was red at ABI resolution since then, so it was only observed after the ABI 0.8 re-sync. The android/kmp plugins deserialize to `serde_json::Value` and read DSL keys via `.get(...)`, so they are unaffected. Added `typed_config_deserializes_from_host_shaped_keys` regression test (19 jvm tests). 19 jvm / 53 android / 24 kmp tests, clippy/fmt clean.
- [x] `ulite/jvm` — KSP output fix (2026-08-29): the `ksp` task declared both `build/generated/ksp/{kotlin,java}` as outputs, but KSP2 creates only the Kotlin dir; a processor that emits no `.java` never creates the java dir, and the host rejected the declared-but-unproduced output (`task 'ksp' declared output '.../build/generated/ksp/java' but it does not exist after the action ran`). The task now declares only the Kotlin output dir; `-java-output-dir` is still passed so KSP generates into it when it does emit java, but it is no longer a required task output. The cannot-compile-generated-Java gap from the observed-issues list is unchanged. No unit test on the output list (register_task is a wit host call; the CI `jvm-ksp` E2E job is the regression test). 19 jvm / 53 android / 24 kmp tests, clippy/fmt clean.

## Next up (priority order)
1. `ulite/kmp` — per-target `implementation`/`api` deps scopes: source-set `deps {}` currently resolve every declaration onto the source set's single classpath; scoping by target is future work
2. ~~`ulite/android` — Compose runtime dependency wiring and BOM support~~ **Resolved host-side** (Uliab 2026-08-23): the host injects the BOM + `runtime`/`ui`/`material3`, and since 2026-08-29 also the `kotlin-compose-compiler-plugin` jar, so android/kmp/jvm modules no longer hand-declare the compiler artifact.
3. `ulite/android` — per-variant source-set layering (androidMain vs debug vs release source-set overrides)

## Observed issues (not fixed, noted for later)
- The `jvm` plugin writes KSP's `-java-output-dir` but has no way to compile generated Java: a processor emitting `.java` produces sources under `build/generated/ksp/java` that no javac task compiles, because javac requires an explicit file list that a task's static inputs cannot enumerate after KSP has run. `kotlinc` (2.2.0, on this JDK) emits no classes for `.java` sources in any invocation shape tested. Documented in REFERENCE.md; a mixed module's javac task could adopt `-sourcepath <generated java dir>` so referenced generated Java compiles — not wired yet.
- Deleting a task's output dir out from under a fingerprint does not invalidate it (host hashes inputs, not output existence) — re-confirmed during ksp E2E: an earlier zero-output ksp run left a fingerprint that made the host skip ksp on later builds until the project state was cleared.
- Core `uliab`'s default registry URL points at `raw.githubusercontent.com/Ulite-Team/ulb-plugins/main/registry/index.json` — the repo is public now, so the default resolves and a custom `--registry` is only needed for local testing.
- AGENTS.md §2 suggests `wit_bindgen::generate!({ inline: ulb_plugin_sdk::WIT, ... })`, but wit-bindgen 0.51's `inline` option parses a string literal only — a const expression fails to compile. Plugins generate via `path:` into the sdk crate's `plugin.wit`, which keeps the single-source-of-truth intent; the AGENTS.md wording should be corrected when the sdk bumps to a wit-bindgen that accepts it.
- The Compose compiler jar must match the Kotlin compiler in use; the host injects it at a pinned version (`composeCompilerVersion`, default `2.3.10`, coordinate `org.jetbrains.kotlin:kotlin-compose-compiler-plugin`). Until the host parses `kotlinc -version` and coordinates the artifact, a compose module's effective compiler version is the pinned default unless the module sets `composeCompilerVersion` — a mismatch between kotlinc and the plugin jar fails at kotlinc runtime with an "unsupported plugin version" style error. No end-to-end `@Composable` compile test exists yet (needs a kotlinc on the CI host). An end-to-end compile is additionally blocked by a classpath gap: the injected Compose `runtime`/`ui`/`material3` artifacts are Android AARs, and the host's resolver materializes only `jar` packaging, so their `@Composable` API classes are not on the compile classpath. (A desktop/jvm compose path would want the `org.jetbrains.compose.*` coordinate family rather than the Android `androidx.compose.*` set; the AAR-unwrapping is a host resolver change and is deferred.)

## Explicitly deferred (out of scope)
- Full task-graph driver in the core `uliab` crate (tracked in Uliab)
- Gradle/.kts compatibility, Kotlin-scripting bridge — not part of the project's scope
- Non-JVM KMP native targets (iOS, desktop) — deferred until cross-compilation toolchain integration is designed
- Windows host support — classpath separator and path conventions need a host-injected contract change
